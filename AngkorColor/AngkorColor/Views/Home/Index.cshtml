@*@model IEnumerable<AngkorColor.Product>*@
@model  PagedList.IPagedList<AngkorColor.Product>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Home Page";
}
@*<link href="~/Content/Style.css" rel="stylesheet" />
<script src="~/Scripts/jquery-2.2.2.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>*@

<div class="BodyHeader">
    <div class="MySlide">
        <div id="myCarousel" class="carousel slide" data-ride="carousel">
            <!-- Indicators -->
            <ol class="carousel-indicators">
                <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                <li data-target="#myCarousel" data-slide-to="1"></li>
                <li data-target="#myCarousel" data-slide-to="2"></li>
                <li data-target="#myCarousel" data-slide-to="3"></li>
            </ol>
            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                <div class="item active">
                    <img height="380" alt="Logo" src="@Url.Content("~/Content/Images/S1.jpg")" />
                </div>
                <div class="item">
                    <img height="380" alt="Logo" src="@Url.Content("~/Content/Images/S3.jpg")" />
                </div>
                <div class="item">
                    <img height="380" alt="Logo" src="@Url.Content("~/Content/Images/S2.jpg")" />
                </div>
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>
</div>

@*@Ajax.ActionLink("Test Ajax", "ListByCat", new AjaxOptions() { HttpMethod = "Get", UpdateTargetId = "CatList", InsertionMode = InsertionMode.Replace }).*@

    
        @*<div class="NewProductsContainer">
            @foreach (var item in Model)
            {
                <div class="col-xs-4 col-md-3">
                    <a href="@Url.Action("Details", "Home", new { id = item.ID })" class="thumbnail">
                        <img src="~/content/images/Products/@item.Picture" alt="...">
                        <div class="caption">
                            <h4>@item.ProductName</h4>
                            <span style="color:red">$ @item.Price</span> <br />
                            <span style="color:gray">@item.Brand.Description</span>
                            <span style="color:gray">@item.ProductCode</span>
                        </div>
                    </a>
                </div>
            }
        </div>*@
        @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page, pagesize = Model.PageSize }))*@

        @*<div class="CatListPro" id="CatList">
            @{Html.RenderPartial("ProListByCat");}
        </div>*@
    
    



